name: Version update

on:
  push:
    branches:
      - test

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Setup GIT
        run: |
          git config --global user.email "rakesh.c@wedaa.tech"
          git config --global user.name "rakesh-wedaa"

      - name: Bump package version
        run: npm version patch -m "Bump version to %s [skip ci]"

      - name: Push vesrion bump to the current branch
        uses: ad-m/github-push-action@v0.6.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main # ${{ github.head_ref }}
          force: true
 